# Optional: route premium TTS through your own proxy endpoint.
# Do not expose provider API keys in Vite `VITE_` variables.
# Leave blank to use browser Web Speech voices only.
VITE_ELEVENLABS_PROXY_URL=
